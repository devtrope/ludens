#!/usr/bin/env php
<?php

require_once __DIR__ . '/../vendor/autoload.php';

use Ludens\Console\Support\Input;
use Ludens\Core\CliApplication;

$cli = new CliApplication();
$cli->boot(__DIR__ . '/..');
$exitCode = $cli->runCommand(new Input());
exit($exitCode);
